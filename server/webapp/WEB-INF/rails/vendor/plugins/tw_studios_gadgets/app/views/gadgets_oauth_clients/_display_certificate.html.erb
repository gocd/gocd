<div class="certificate_details">
  <div><h3>Certificate Details:</h3></div>
  <div>

    <h4 class="certificate_section_header">Issued to:</h4>
    <dl>
      <dt>Common Name (CN)</dt>
      <dd id="cert-subject-cn"><%= h @cert.subject_dn.common_name %></dd>

      <dt>Organization (O)</dt>
      <dd id="cert-subject-org"><%= h @cert.subject_dn.organization %></dd>

      <dt>Organizational Unit (OU)</dt>
      <dd id="cert-subject-org-unit"><%= h @cert.subject_dn.organizational_unit %></dd>

      <dt>Serial Number</dt>
      <dd id="cert-subject-serial-number"><%= h @cert.serial_number_string %></dd>
    </dl>


    <h4 class="certificate_section_header">Issued by:</h4>
    <dl>
      <dt>Common Name (CN)</dt>
      <dd id="cert-issuer-cn"><%= h @cert.issuer_dn.common_name %></dd>

      <dt>Organization (O)</dt>
      <dd id="cert-issuer-org"><%= h @cert.issuer_dn.organization %></dd>

      <dt>Organizational Unit (OU)</dt>
      <dd id="cert-issuer-org-unit"><%= h @cert.issuer_dn.organizational_unit %></dd>
    </dl>

    <h4 class="certificate_section_header">Validity:</h4>
    <dl>
      <dt>Issued On</dt>
      <dd id="cert-issued-on"><%= h @cert.issued_on %></dd>

      <dt>Expires On</dt>
      <dd id="cert-expires-on"><%= h @cert.expires_on %></dd>

    </dl>

    <h4 class="certificate_section_header">Fingerprints:</h4>

    <dl>
      <dt>SHA1 fingerprint</dt>
      <dd id="cert-sha1"><%= h @cert.sha1_fingerprint %></dd>

      <dt>MD5 fingerprint</dt>
      <dd id="cert-md5"><%= h @cert.md5_fingerprint %></dd>

    </dl>
  </div>
</div>
<div class="form_item accept_certificate_wrapper">
  <div class="info-box">
    <input type="checkbox" value="accept_certificate" id="accept_certificate" name="accept_certificate"/>
    <label for="accept_certificate" class="accept_certificate">Accept this certificate</label>
    <input type="hidden" value="<%= Gadgets.truststore_checksum %>" id="truststore_checksum" name="truststore_checksum"/>
  </div>
</div>
