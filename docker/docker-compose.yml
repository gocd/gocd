services:
  trivy:
    image: docker.io/aquasec/trivy:0.71.0@sha256:016eae51fdcf989332a5404af7e8f625cd5d95d7c0907a221d080a996f556500
    command: [ image, --input, /scan-image, --exit-code, "${EXIT_CODE_ON_ERROR}", --config, /trivy.yaml ]
    volumes:
      - ./trivy.yaml:/trivy.yaml:ro
      - trivycache:/root/.cache
volumes:
  trivycache:
