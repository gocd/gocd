source 'https://rubygems.org'
gemspec
gem 'cucumber', '~>1.3.2'
gem 'rspec', '~>2.13.0'
gem 'rake'
gem "spork", :path => File.expand_path("../", __FILE__)

if RUBY_VERSION =~ /^2\.0|^1\.9/
  gem 'debugger'
else
  gem 'ruby-debug'
end
