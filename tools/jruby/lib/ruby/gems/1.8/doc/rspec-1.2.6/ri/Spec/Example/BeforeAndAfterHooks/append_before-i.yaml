--- !ruby/object:RI::MethodDescription 
params: (scope = :each, &block)
is_singleton: false
block_params: 
name: append_before
comment: 
- !ruby/struct:SM::Flow::P 
  body: Registers a block to be executed before examples. <tt>scope</tt> can be <tt>:each</tt>
    (default), <tt>:all</tt>, or <tt>:suite</tt>. When <tt>:each</tt>, the block is
    executed before each example. When <tt>:all</tt>, the block is executed only once
    before any examples are run.
full_name: Spec::Example::BeforeAndAfterHooks#append_before
visibility: public
aliases: 
- !ruby/object:RI::AliasName 
  name: before
